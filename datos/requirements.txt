# Dependencias para descarga y extracción de Wikipedia
# Instalar con: pip install -r requirements.txt

requests>=2.28.0      # Descarga de archivos HTTP
tqdm>=4.64.0          # Barra de progreso
wikiextractor>=3.0.6  # Extracción de artículos del dump XML
